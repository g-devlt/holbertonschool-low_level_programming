#include <stdio.h>

/**
 * main - Prints the program name
 * @argc: Argument count
 * @argv: Argument values
 * Return: 0 on success
*/
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
